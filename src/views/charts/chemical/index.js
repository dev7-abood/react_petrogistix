import Breadcrumbs from '@components/breadcrumbs'
import {
    Label, Input, Card, CardHeader, CardBody, Row, CardTitle, Col, Form, FormGroup, Button, Progress
} from 'reactstrap'

const index = _ => {
    return (
        <>
            <Breadcrumbs breadCrumbTitle='Chemical Chart view' breadCrumbParent='Dashboard' breadCrumbActive='Chemical Chart view'/>
            <Card>
                <CardBody>
                    Z
                </CardBody>
            </Card>
        </>
    )
}

export default index